/**
 * ZukusItem - Custom Item class for D&D 3.5e items
 */

export class ZukusItem extends Item {
  /**
   * Prepare data for the item.
   */
  prepareData(): void {
    super.prepareData();
  }

  /**
   * Prepare derived data for the item.
   */
  prepareDerivedData(): void {
    super.prepareDerivedData();
  }
}
