export enum costTypes {
    BONUS,
    MONEY
}

enum auraTypes {
    FAINT,
    MODERATE,
    STRONG,
    OVERWHELMING
}

enum magicSchools {
    ABJURATION,
    CONJURATION,
    DIVINATION,
    ENCHANTMENT,
    EVOCATION,
    ILLUSION,
    NECROMANCY,
    TRANSMUTATION
}

enum activationTypes {
    NONE,
    ACTION_COMMAND,
    ACTION_MENTAL,
}

enum actionTypes {
    FREE,
    IMMEDIATE,
    SWIFT,
    MOVE,
    STANDARD,
    FULLROUND,
}