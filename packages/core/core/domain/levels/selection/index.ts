/**
 * Selection functions for managing selector state
 * 
 * These functions handle adding/removing entities from selectors
 * and validating the selector's current state.
 */

export * from './types';
export { applySelection } from './applySelection';
export { removeSelection } from './removeSelection';
export { validateSelector } from './validateSelector';

