/**
 * Storage module for character entity data.
 * 
 * Exports types for storing entities in the character's base data.
 */

export type {
  // Entity storage
  EntityInstance,
  EntityOrigin,
  LevelSlot,
  
  // Class entity
  ClassEntity,
  ClassLevelRow,
  BabProgression,
  SaveProgression,
  SaveProgressions,
  ClassType,
} from './types';

